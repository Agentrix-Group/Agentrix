{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AgentAction",
  "description": "Schema for autonomous agent bot decisions per turn in Agentrix",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["UP", "DOWN", "LEFT", "RIGHT", "ATTACK", "SHIELD", "REST"],
      "description": "The atomic action command executed by the agent"
    },
    "payload": {
      "type": "object",
      "description": "Optional action metadata or target parameters",
      "additionalProperties": true
    },
    "debug_message": {
      "type": "string",
      "description": "Optional debug or telemetry message recorded in match log"
    }
  },
  "required": ["type"]
}

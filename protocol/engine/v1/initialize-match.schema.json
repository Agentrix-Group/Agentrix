{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentrix.dev/schemas/engine/v1/initialize-match.schema.json",
  "title": "InitializeMatchPayload",
  "description": "Payload enviado por Agentrix hacia el motor para configurar e iniciar una partida.",
  "type": "object",
  "required": [
    "matchId",
    "gameId",
    "seed",
    "fixedTimestepMs",
    "maxTicks",
    "players"
  ],
  "properties": {
    "matchId": {
      "type": "string",
      "minLength": 1
    },
    "gameId": {
      "type": "string",
      "minLength": 1
    },
    "expectedEngineVersion": {
      "type": "string"
    },
    "seed": {
      "type": "integer",
      "description": "Semilla numérica determinista para el generador pseudoaleatorio."
    },
    "fixedTimestepMs": {
      "type": "integer",
      "minimum": 1,
      "description": "Duración de cada tick en milisegundos."
    },
    "maxTicks": {
      "type": "integer",
      "minimum": 1,
      "description": "Límite superior de ticks antes de finalizar automáticamente la partida."
    },
    "players": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Lista ordenada de identificadores de participantes o slots."
    },
    "config": {
      "type": "object",
      "description": "Snapshot de configuración específica del juego.",
      "additionalProperties": true
    },
    "participantArtifactDigests": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Digests criptográficos (sha256) de los artefactos de cada participante para auditoría."
    }
  },
  "additionalProperties": false
}

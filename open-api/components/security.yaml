bearerAuth:
  type: http
  scheme: bearer
  bearerFormat: JWT
  description: JWT Access token passed in Authorization header as 'Bearer <token>'

sessionAuth:
  type: apiKey
  in: cookie
  name: session-id
  description: Optional session cookie created after login.

# Demo-only configuration for docker compose. These values are public and
# MUST NOT be used for any real deployment (MODE=demo refuses placeholder
# secrets, but these are only protected by being demo-scoped).
MODE=demo
LOG_LEVEL=info
LOG_FORMAT=json
DB_HOST=postgres
DB_PORT=5432
DB_USER=agentrix
DB_PASSWORD=agentrix-demo-db-password
DB_NAME=agentrix
DB_SSLMODE=disable
ACCESS_SECRET=agentrix-demo-access-7f3c9b2e41d85a60c1e9f4b8d2a7
COOKIE_SECURE=false
COOKIE_SAMESITE=strict
TRUSTED_PROXIES=172.16.0.0/12,10.0.0.0/8,192.168.0.0/16
REGISTRATION_OPEN=true
DEMO_ADMIN_USERNAME=admin
DEMO_ADMIN_PASSWORD=agentrix-demo-admin
DEMO_PLAYER_USERNAME=player
DEMO_PLAYER_PASSWORD=agentrix-demo-player
